# Core environment management
python-dotenv

# Streamlit for the frontend UI
streamlit

# Pinecone vector store
pinecone-client

# LangChain and dependencies
langchain
langchain-openai
langchain-pinecone
langchain-community

# PDF document loading
PyMuPDF  # used by PyPDFDirectoryLoader internally

# Use OpenRouter-compatible HTTP library
httpx

# Optional (but useful for debugging)
tqdm
